#include <iostream>
#include <string>

using namespace std;
char Word[50];
int polovina, length = 0, sim;
bool Palindrom = true;

int main() {
    cout << "Enter a word:";
    cin >> Word;
    while (Word[sim])//dovjina
        length++;
    sim = (int)(length / 2);

}